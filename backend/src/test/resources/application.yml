spring:
  datasource:
    url: jdbc:h2:mem:autotest_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  cors:
    allowed-origins: http://localhost:5173
  seed:
    admin-username: testadmin
    admin-password: testadmin123

security:
  jwt:
    secret: test-secret-key-must-be-at-least-32-chars
    expiration-seconds: 3600
