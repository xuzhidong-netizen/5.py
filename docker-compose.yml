version: "3.9"

services:
  autotest:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./data/autotest.db
      AUTO_SEED_DEMO: "true"
      REQUEST_TIMEOUT_SECONDS: "15"
    volumes:
      - ./data:/app/data
